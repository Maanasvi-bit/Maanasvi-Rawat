Assuming you have a C source file named example.c, let's compile it into an executable.

1. Write your C program
// example.c

#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}

2.Open Terminal

Navigate to the directory containing your C file

cd /path/to/your/directory

4. Compile the C file

gcc -o example example.c

Compiling on Windows (using gcc via MinGW or Cygwin):
gcc -o example.exe example.c

5. List files in the directory
ls      # On Unix-like systems
dir     # On Windows

6. Run the executable
./example       # On Unix-like systems
example.exe     # On Windows



